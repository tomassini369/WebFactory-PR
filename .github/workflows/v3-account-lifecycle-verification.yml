name: V3 Account Lifecycle Verification
on:
  push:
    branches:
      - feature/webfactory-v3-business-platform
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm test
      - run: npm run build
      - run: npx netlify-cli build --offline
